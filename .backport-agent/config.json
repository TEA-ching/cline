{
    "command": "npx @sctg/backport-agent@latest --verbose --config .backport-agent/config.json --backport-customizations .backport-agent/customizations.yaml --keypool-vault-url $KEYPOOL_VAULT_URL --keypool-live-secret $KEYPOOL_LIVE_SECRET",
    "report": {
        "destination": ".backport-agent"
    },
    "auth": {
        "sshKeyPath": "~/.ssh/id_ed25519_aeltorio"
    },
    "upstream": {
        "url": "https://github.com/cline/cline.git",
        "repo": "cline/cline.git",
        "branch": "main",
        "remote": "upstream"
    },
    "fork": {
        "url": "git@github.com:TEA-ching/cline.git",
        "repo": "TEA-ching/cline",
        "branch": "keypool-live",
        "remote": "origin"
    },
    "workingDir": "../tea-ching-cline",
    "sync": {
        "maxCommitsPerRun": 10,
        "initialFetchDepth": 200,
        "maxFetchDepth": 2000,
        "batchSize": 3,
        "dryRun": false,
        "createPullRequest": false,
        "branchPrefix": "sync/upstream-"
    },
    "models": {
        "fast": "mistral/devstral-latest",
        "specialist": "gemini/gemini-3-flash-preview",
        "powerful": "openai/gpt-5.5"
    },
    "resolve": {
        "ours": [],
        "theirs": []
    },
    "validation": {
        "low": [
            "pushd apps/vscode && bun install && npm run check-types ; popd",
            "pushd sdk && bun install && npm run types ; popd"
        ],
        "medium": [
            "pushd apps/vscode && bun install && npm run check-types ; popd",
            "pushd apps/vscode && bun install && npm run test:unit ; popd",
            "pushd sdk && bun install && npm run types ; popd",
            "pushd sdk && bun install && npm run test:unit ; popd"
        ],
        "high": [
            "pushd apps/vscode && bun install && npm run check-types ; popd",
            "pushd apps/vscode && bun install && npm run check-types ; popd",
            "pushd apps/vscode && bun install && npm run test:unit ; popd",
            "pushd apps/vscode && bun install && npm run compile ; popd",
            "pushd apps/vscode && bun install && npm run package:vsix ; popd"
        ]
    }
}
